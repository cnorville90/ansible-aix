---
- name: Configure the inittab on an AIX Machine
  hosts: aix
  become: true
  gather_facts: false

  tasks: 

    - name: Install packages in the packages list
      ansible.builtin.dnf:
        name: "{{ item }}"
        state: latest
        use_backend: dnf4
      loop:
        - packages
